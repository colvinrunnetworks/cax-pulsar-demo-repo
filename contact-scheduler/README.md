# contact-scheduler — inert component

This directory contains only a dependency manifest. There is **no source code, no
entrypoint, and nothing to run** here. It exists so COSMIC-AX PULSAR can parse the
manifest during its demo scan. See the repository [README](../README.md).
